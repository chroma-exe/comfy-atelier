import { app } from "../../scripts/app.js";

// the frontend seam. inert until phase 2 brings the + Add Section machinery.
// if you're reading this and it still does nothing, that's on purpose, not abandonment.
app.registerExtension({
    name: "atelier.hub",
});
